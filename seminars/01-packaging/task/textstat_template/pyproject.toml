# =====================================================================
#  textstat — пакет семинара 1. Шесть TODO, закрой все.
# =====================================================================
#
#  Скучные поля (автор, лицензия, классификаторы, ссылки) уже заполнены —
#  там нечего понимать, просто посмотри, как они выглядят.
#
#  Проверять по ходу:
#      uv build                 # соберётся sdist + wheel в dist/
#      unzip -l dist/*.whl      # что РЕАЛЬНО попало в колесо
#
#  Главное правило семинара: «собралось» ≠ «собралось правильно».

# ---------------------------------------------------------------------
# TODO 1. Чем собирать.
#
#   Раскомментируй и заполни. Backend на выбор — hatchling или setuptools.
#   От выбора зависит TODO 6: один кладёт не-.py файлы в колесо сам,
#   другой требует явной настройки.
#
#   Осторожно: без этой секции сборка НЕ упадёт — сработает исторический
#   fallback на setuptools, и колесо соберётся. Только неправильное.
# ---------------------------------------------------------------------
# [build-system]
# requires = [...]
# build-backend = "..."


[project]
# TODO 2. Имя дистрибутива: textstat-seminar-<твой-github-ник>, нижний регистр.
#   На TestPyPI имя должно быть свободно — отсюда суффикс.
#   Имя ИМПОРТА при этом остаётся textstat_seminar у всех. Это разные вещи.
name = "textstat-seminar-CHANGEME"

# TODO 3. Версия. Проще всего — строкой:
#       version = "0.1.0"
#   Красивее — взять из кода, чтобы правда была в одном месте:
#       dynamic = ["version"]
#   плюс секция backend'а, которая скажет, откуда читать __version__
#   (он лежит в src/textstat_seminar/__init__.py). Делай любой вариант.
version = "0.0.0"

# TODO 4. Описание пакета: короткое в description, длинное — файлом.
#   README.md в этой папке уже готов, его надо только подключить.
description = ""
# readme = ...


# ----- дальше заполнено за тебя, менять не обязательно -----

requires-python = ">=3.10"
authors = [{ name = "Student", email = "student@example.com" }]  # замени на своё
license = "MIT"
license-files = ["LICENSE"]
keywords = ["text", "statistics", "cli", "education"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Education",
  "Programming Language :: Python :: 3",
  "Topic :: Text Processing :: Linguistic",
]
# Пакету ничего не нужно, кроме стандартной библиотеки.
dependencies = []

[project.urls]
Homepage = "https://github.com/DanielShinoda/ami_advanced_python_seminars_26f"


# ---------------------------------------------------------------------
# TODO 5. Точка входа: команда `textstat` должна появиться после установки.
#
#   Секция [project.scripts], формат:
#       имя-команды = "модуль:функция"
#   Нужная функция — main() в textstat_seminar/cli.py.
#
#   Проверка: после установки `textstat --version` работает из любой папки.
#   Ломается МОЛЧА: сборка пройдёт, пакет опубликуется, а команды не будет.
# ---------------------------------------------------------------------


# ---------------------------------------------------------------------
# TODO 6. Файлы стоп-слов должны попасть в колесо.
#
#   src/textstat_seminar/data/stopwords_{ru,en}.txt — не Python-код, и не
#   каждый backend положит их в wheel сам.
#
#     1) собери                        uv build
#     2) посмотри                      unzip -l dist/*.whl
#     3) если .txt там нет — настрой backend
#
#   Что искать в документации своего backend'а:
#     hatchling  → [tool.hatch.build.targets.wheel] packages
#     setuptools → [tool.setuptools.package-data]
#
#   Тоже ломается молча: локально работает, после установки —
#   FileNotFoundError.
# ---------------------------------------------------------------------
